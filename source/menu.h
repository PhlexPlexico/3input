#ifndef MENU_H
#define MENU_H
/*
#include <3ds.h>

struct menu_option_t {
    struct menu_option_t next;
    struct menu_option_t prev;
    struct menu_option_t parent;
    char* text;
    void* data
};

typedef struct{
    PrintConsole console;
    int cursor;
    struct menu_option_t* root;
    struct menu_option_t* curr;
} menu_t;

int menu_ctor(menu_t*);
int menu_dtor(menu_t*);

void menu_draw(menu_t*);
*/
#endif