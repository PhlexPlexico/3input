#include "menu.h"

/* 
int menu_ctor(menu_t* menu, struct menu_option_t *root) {
    consoleInit(GFX_TOP, &(menu->console));
    menu->root = root;
    menu->curr = menu->root;
    cursor = 0;
}
int menu_dtor(menu_t*) {

}

void menu_draw(menu_t*) {
    
}*/